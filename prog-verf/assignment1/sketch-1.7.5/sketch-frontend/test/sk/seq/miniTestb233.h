#ifndef MINITESTB233_H
#define MINITESTB233_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void testSK0(int N, int* in/* len = N */, int* _out/* len = N */);
extern void test0(int N, int* in/* len = N */, int* _out/* len = N */);
}

#endif
