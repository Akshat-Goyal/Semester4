#ifndef MINITESTB245_H
#define MINITESTB245_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void testSK0(int N, float* a/* len = N */, float* b/* len = N */, float* _out/* len = N */);
extern void test0(int N, float* a/* len = N */, float* b/* len = N */, float* _out/* len = N */);
}

#endif
