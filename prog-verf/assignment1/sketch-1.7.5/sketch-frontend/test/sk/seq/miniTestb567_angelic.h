#ifndef MINITESTB567_ANGELIC_H
#define MINITESTB567_ANGELIC_H

#include <cstring>

#include "vops.h"

namespace generators{
}
namespace generators{
extern void partition__Wrapper(int p, int P, int N);
extern void partition__WrapperNospec(int p, int P, int N);
extern void partition(int p, int P, int N);
extern void calc(int len, int N, int* in_0/* len = N */, int* rator/* len = 5 */, int* xrand/* len = 5 */, int* yrand/* len = 5 */, int* _out/* len = 6 */);
extern void moo(int len, int N, int* in/* len = N */, int* rator/* len = 5 */, int* xrand/* len = 5 */, int* yrand/* len = 5 */, int* _out/* len = 6 */);
}

#endif
