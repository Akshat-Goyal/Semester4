#ifndef MINITESTB263_H
#define MINITESTB263_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void testSK9(int N, int* in/* len = N */, int* _out/* len = N */);
extern void test9(int N, int* in/* len = N */, int* _out/* len = N */);
}

#endif
