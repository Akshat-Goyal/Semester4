#ifndef MINITESTB701_H
#define MINITESTB701_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void mainB(bool* x/* len = 3 */, bool* y/* len = 3 */, bool* _out/* len = 3 */);
extern void _main(bool* x/* len = 3 */, bool* y/* len = 3 */, bool* _out/* len = 3 */);
}

#endif
