#ifndef MINITEST16_H
#define MINITEST16_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void reverseSketch(bool* in_0/* len = 4 */, bool* _out/* len = 4 */);
extern void reverse(bool* in_0/* len = 4 */, bool* _out/* len = 4 */);
}

#endif
