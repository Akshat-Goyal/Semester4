#ifndef MINITEST54_H
#define MINITEST54_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void tableSum(bool* in1/* len = 2 */, bool* in2/* len = 2 */, bool* _out/* len = 32 */);
extern void sum(bool* in1/* len = 2 */, bool* in2/* len = 2 */, bool* _out/* len = 32 */);
}

#endif
