#ifndef MINITEST38_H
#define MINITEST38_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void miniTestSK(bool* in_0/* len = 4 */, bool* in2/* len = 2 */, bool* _out/* len = 4 */);
extern void miniTest(bool* in_0/* len = 4 */, bool* in2/* len = 2 */, bool* _out/* len = 4 */);
}

#endif
