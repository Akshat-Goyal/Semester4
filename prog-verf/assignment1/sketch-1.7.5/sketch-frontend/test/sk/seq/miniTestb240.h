#ifndef MINITESTB240_H
#define MINITESTB240_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void stenSK14(int N, int a, float* in/* len = N * N */, float* _out/* len = N * N */);
extern void sten14(int N, int a, float* in/* len = N * N */, float* _out/* len = N * N */);
}

#endif
