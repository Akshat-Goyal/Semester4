#ifndef MINITEST17_H
#define MINITEST17_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void reverseSketch(bool* in/* len = 4 */, bool* _out/* len = 5 */);
extern void reverse(bool* in/* len = 4 */, bool* _out/* len = 5 */);
}

#endif
