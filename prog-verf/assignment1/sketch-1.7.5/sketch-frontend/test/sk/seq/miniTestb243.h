#ifndef MINITESTB243_H
#define MINITESTB243_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void testSK(int N, int* in/* len = N */, int* _out/* len = N */);
extern void test(int N, int* in/* len = N */, int* _out/* len = N */);
}

#endif
