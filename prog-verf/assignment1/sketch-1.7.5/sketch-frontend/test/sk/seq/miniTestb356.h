#ifndef MINITESTB356_H
#define MINITESTB356_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void sumsk(int N, int L, bool* x/* len = L */, int M, bool* y/* len = M */, bool* _out/* len = N */);
extern void sum(int N, int L, bool* x/* len = L */, int M, bool* y/* len = M */, bool* _out/* len = N */);
}

#endif
