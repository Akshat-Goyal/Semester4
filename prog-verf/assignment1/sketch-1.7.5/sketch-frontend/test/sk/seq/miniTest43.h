#ifndef MINITEST43_H
#define MINITEST43_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void mergeSort(bool* input/* len = 2 */, bool* _out/* len = 2 */);
extern void sort(bool* input/* len = 2 */, bool* _out/* len = 2 */);
}

#endif
