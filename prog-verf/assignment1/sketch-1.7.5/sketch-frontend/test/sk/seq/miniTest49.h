#ifndef MINITEST49_H
#define MINITEST49_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void reverseSketch(bool* in/* len = 4 */, bool* _out/* len = 4 */);
extern void reverse(bool* in/* len = 4 */, bool* _out/* len = 4 */);
}

#endif
