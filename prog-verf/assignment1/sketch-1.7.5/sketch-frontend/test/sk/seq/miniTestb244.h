#ifndef MINITESTB244_H
#define MINITESTB244_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void rec6ptSk(int N_, float* in_/* len = N_ * N_ */, float* _out/* len = N_ * N_ */);
extern void rec6pt(int N_, float* in_/* len = N_ * N_ */, float* _out/* len = N_ * N_ */);
}

#endif
