#ifndef MINITEST26_H
#define MINITEST26_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void miniTestSK(bool* in/* len = 3 */, bool* _out/* len = 3 */);
extern void miniTest(bool* in/* len = 3 */, bool* _out/* len = 3 */);
}

#endif
