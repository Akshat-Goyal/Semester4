#ifndef MINITESTB262_H
#define MINITESTB262_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void testSK8(int N, int* in/* len = N */, int a, int b, int* _out/* len = N */);
extern void test8(int N, int* in/* len = N */, int a, int b, int* _out/* len = N */);
}

#endif
