#ifndef MINITESTB251_H
#define MINITESTB251_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void testSK0(int N, float* a/* len = N * N */, float* _out/* len = N * N */);
extern void test0(int N, float* a/* len = N * N */, float* _out/* len = N * N */);
}

#endif
