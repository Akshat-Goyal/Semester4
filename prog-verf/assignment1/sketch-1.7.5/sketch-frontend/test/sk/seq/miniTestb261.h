#ifndef MINITESTB261_H
#define MINITESTB261_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void testSK7(int N, int* in/* len = N * N */, int* _out/* len = N * N */);
extern void test7(int N, int* in/* len = N * N */, int* _out/* len = N * N */);
}

#endif
