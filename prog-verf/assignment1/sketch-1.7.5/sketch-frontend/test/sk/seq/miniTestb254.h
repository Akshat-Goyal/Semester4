#ifndef MINITESTB254_H
#define MINITESTB254_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void stenSK13(int N, float* in/* len = N * N */, int a, float* _out/* len = N * N */);
extern void sten13(int N, float* in/* len = N * N */, int a, float* _out/* len = N * N */);
}

#endif
