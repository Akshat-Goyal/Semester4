#ifndef MINITEST78_H
#define MINITEST78_H

#include <cstring>

#include "vops.h"

namespace ANONYMOUS{
}
namespace ANONYMOUS{
extern void sketch(bool* in1_0/* len = 5 */, int in2, bool* _out/* len = 5 */);
extern void spec(bool* in1_0/* len = 5 */, int in2, bool* _out/* len = 5 */);
}

#endif
